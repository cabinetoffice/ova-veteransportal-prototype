version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands: 
        - npm run serve
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /app/views
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
